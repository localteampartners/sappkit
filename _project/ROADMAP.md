# ROADMAP — sappkit

<!-- UPDATE WHEN: a theme ships and moves to "Now → Next → Later", a new theme is added, or horizons shift materially. NOT for individual tasks (those go in TODO.md) or static goals (those live in SPEC.md). -->

Themed, time-horizoned view of where this project is going.

For tactical work items, see [TODO.md](TODO.md).

---

## Now (this month)

- **Library coverage** — SM Drums + Big Rusty verified through the pad
  mapper; VSCO2 percussion SFZ generator. Success: three real kits playable
  out of the box.
- **Presets** — save/recall kit-bus + pad-override sets per SFZ.

## Next (1–3 months)

- **sapptune integration** — groove specs rendered through sappkit via the
  SappLink manifest tags (punch/space/lofi addressed generically).
- **Pad workflow** — mute/solo, choke-group overrides, velocity curves.

## Later (3–6+ months)

- **Multi-out** — pad → bus routing for DAW mixing.
- **Kit morph / layering** — two SFZ kits blended per pad.

## Considered, not doing

- Hall reverb — deliberately a tight room; DAW sends cover the rest.
- Built-in sequencer — sapptune owns composition.
- Bundled samples — licenses + repo size; the fetch registry covers it.
